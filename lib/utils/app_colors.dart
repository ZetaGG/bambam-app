import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // AppBar
  static const Color appBarBackground = Color(0xFFA39381);

  // SliverAppBar (HomePage)
  static const Color sliverAppBarBackground = Color(0xFFD8D6D4);
  static const Color sliverAppBarForeground = Color(0xFF2B1F15);

  // NavigationBar
  static const Color navBarBackground = Color(0xFFDDD5CA);

  // Status badges
  static const Color available = Color(0xFF598F55);
}
